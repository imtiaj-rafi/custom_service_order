<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">
        <!-- Record rule for Sales: Own Documents Only -->
        <record id="sale_service_order_own_rule" model="ir.rule">
            <field name="name">Service Orders: Own Documents Only</field>
            <field name="model_id" ref="model_sale_service_order"/>
            <field name="domain_force">[('salesman_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
        </record>
    </data>
</odoo>
